##############################################################################
# Makefile — AI-VOX3 board (top)
#
# Canonical NuttX board Makefile: sets APPNAME and includes the per-source
# Makefile. Align with the esp32s3-eye board's existing Makefile if needed.
##############################################################################

include $(TOPDIR)/.config
include $(TOPDIR)/tools/Config.mk

APPNAME = board

include src/Makefile
