# =============================================================================
# AI-VOX3 (emakefun) — Config: nsh
# Minimal NSH shell over native USB-CDC (VID:PID = 303A:1001)
# Board: ESP32-S3-R8, 16MB Flash, 8MB OPI PSRAM
# NOTE: This defconfig is derived from esp32s3-eye/nsh and adapted to AI-VOX3.
#       Some symbols must be re-verified in `menuconfig` on the build machine.
# =============================================================================

CONFIG_ARCH="xtensa"
CONFIG_ARCH_CHIP="esp32s3"
CONFIG_ARCH_CHIP_ESP32S3=y
CONFIG_ARCH_CHIP_ESP32S3WROOM1N16R8=y

CONFIG_ARCH_BOARD="ai_vox3"
CONFIG_ARCH_BOARD_AI_VOX3=y

CONFIG_ESP32S3_DEFAULT_CPU_FREQ_240=y
CONFIG_ESP32S3_FLASH_16M=y
CONFIG_ESP32S3_SPIRAM=y
CONFIG_ESP32S3_SPIRAM_MODE_OCT=y
CONFIG_ESP32S3_SPIRAM_SPEED_80M=y

# --- Console: native USB-CDC (NOT UART0) -------------------------------------
# AI-VOX3 exposes the ESP32-S3 ROM USB as a CDC-ACM serial console.
CONFIG_ESP32S3_USBSERIAL=y
CONFIG_ESP32S3_CDCACM=y
CONFIG_CDCACM=y
CONFIG_CDCACM_CONSOLE=y
CONFIG_UART0_SERIAL_CONSOLE=n
# TODO(real-device): verify USB-CDC console selection & VID:PID on the real
# board (doc says 303A:1001). If console does not appear, confirm the exact
# CDCACM symbol names in the current OpenVela tree via `menuconfig`.

# --- NSH base ----------------------------------------------------------------
CONFIG_BOARDCTL=y
CONFIG_BUILTIN=y
CONFIG_FS_PROCFS=y
CONFIG_FS_PROCFS_REGISTER=y
CONFIG_INIT_ENTRYPOINT="nsh_main"
CONFIG_NSH_ARCHINIT=y
CONFIG_NSH_BUILTIN_APPS=y
CONFIG_NSH_FILE_APPS=y
CONFIG_NSH_READLINE=y
CONFIG_NSH_ROMFSETC=y
CONFIG_NSH_ROMFSRC=y
CONFIG_NSH_SCRIPT=y
CONFIG_SYSTEM_NSH=y
CONFIG_EXAMPLES_NSH=y

CONFIG_SCHED_WAITPID=y
CONFIG_RR_INTERVAL=200
CONFIG_SDCLONE_DISABLE=y

CONFIG_START_YEAR=2026
CONFIG_START_MONTH=1
CONFIG_START_DAY=1
